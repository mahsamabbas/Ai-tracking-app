# Identity is not stored here. Pair this computer from the dashboard:
# Access → Connect this computer next to the person using this Mac.
#
# Hosted API (Vercel project tracking-app-api). Omit to use dashboard pairing apiBaseUrl.
TECHLIO_API_URL=https://tracking-app-api-three.vercel.app
# Dashboard origins allowed to call this connector (comma-separated).
TECHLIO_DASHBOARD_ORIGINS=https://tracking-app-api-t9yd.vercel.app
# CONNECTOR_PORT=9477
#
# Employees: use the dashboard Install agent page (Node.js only, no clone).
# Developers with this repo: pnpm dev:connector
