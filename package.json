{
  "name": "techlio-ai-activity",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "test": "turbo run test && pnpm --filter @techlio/e2e test",
    "lint": "turbo run lint",
    "db:migrate": "pnpm --filter @techlio/api exec drizzle-kit migrate"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
